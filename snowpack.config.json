{
  "scripts": {
    "run:css": "postcss scss/*.scss --dir docs/css --ext css",
    "run:css::watch": "$1 --watch",
    "mount:public": "mount docs --to /",
    "mount:backend": "mount backend --to /backend",
    "run:backend": "nodemon backend/index.js"
  }
}
